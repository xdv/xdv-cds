# Architecture
XDV Cross-Domain Scheduler is structured in two layers:
1. Stable interface layer (src/cds_interface.ds)
2. Implementation layer (src/cds.ds)
Tests from the source sector are imported into src and can be mirrored into tests as independent suites over time.
